#include <stdlib.h>
#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char c = 'A';
	char l = 'a';

	while (l <= 'z')
	{
		putschar(l);
		l++;
	}
	while (c <= 'Z')
	{
		putschar(c);
		c++;
	}
	putchar('\n');
	return (0);
}
