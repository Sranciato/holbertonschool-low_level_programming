#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{

	int a = 0;

	if (a > 0)
		printf("%d is positive\n", a);
	if else(a = 0)
		printf("%d is zero\n", a);
	else
		printf("%d is negative\n", a);
	return (0);
}
